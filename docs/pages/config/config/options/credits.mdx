## **credits**
차트 제작 주체 등을 표시하는 영역에 대한 모델.<br/>
기본적으로 차트 오른쪽 아래에 표시되지만, [align](#align) , [verticalAlign](#verticalalign) 등으로 위치를 지정할 수 있다. [url](#url) 을 설정하면 마우스 클릭 시 새창을 열고 해당 위치로 이동시킨다.
## Properties

### align: `'center'|'left'|'right'{:js}`[#align]
- `'center'` 
- `'left'` 
- `'right'` 

<DefaultValue value="'right'" contents=""></DefaultValue>
### floating: `boolean{:js}`[#floating]
<Content type="keyword" value="true"></Content>이면 별도의 영역을 차지하지 않고 chart view위에 표시된다.<br/>  
<DefaultValue value="false" contents=""></DefaultValue>
### offsetX: `number{:js}`[#offsetX]
[align](#align) 으로 지정된 수평 위치에서, 양수로 지정하면 안쪽으로 음수면 바깥쪽으로 밀어서 표시한다.  
<DefaultValue value="2" contents=""></DefaultValue>
### offsetY: `number{:js}`[#offsetY]
[verticalAlign](#verticalalign) 으로 지정된 수직 위치에서, 양수로 지정하면 안쪽으로 음수면 바깥쪽으로 밀어서 표시한다.  
<DefaultValue value="1" contents=""></DefaultValue>
### style: `SVGStyles | string{:js}`[#style]
스타일셋 혹은 class selector.  
- `SVGStyles{:js}` 

| Name | Type | Optional |  
| ----- | ----- | ----- |  
| fill | `string{:js}` | true |  
| fillOpacity | `string{:js}` | true |  
| fontFamily | `string{:js}` | true |  
| fontSize | `string{:js}` | true |  
| fontStyle | `string{:js}` | true |  
| fontWeight | `string{:js}` | true |  
| rx | `string{:js}` | true |  
| stroke | `string{:js}` | true |  
| strokeDasharray | `string{:js}` | true |  
| strokeWidth | `string{:js}` | true |  
| textAlign | `'center' \| 'left' \| 'right'{:js}` | true |<DefaultValue value="undefined" contents=""></DefaultValue>
### text: `string{:js}`[#text]
표시할 문자열.  
<DefaultValue value="'RealChart" contents="v1.0'"></DefaultValue>
### url: `string{:js}`[#url]
이 속성을 지정하면 click시 해당 새 창(혹은 tab)을 열어서 해당 위치로 이동한다.  
<DefaultValue value="'http://realgrid.com'" contents=""></DefaultValue>
### verticalAlign: `'bottom'|'middle'|'top'{:js}`[#verticalAlign]
- `'bottom'` 
- `'middle'` 
- `'top'` 

<DefaultValue value="'bottom'" contents=""></DefaultValue>
### visible: `boolean{:js}`[#visible]
표시 여부.  
<DefaultValue value="true" contents=""></DefaultValue>