## scale
scale.
## Properties

### [line](./linear/line)
RealReport-Chart 라이브러리 클래스 모델의 최상위 base 클래스.

  

### position[#position]


`default: GaugeItemPosition.DEFAULT`  

### stepCount: `number{:javascript}`[#stepCount]

### stepInterval: `number{:javascript}`[#stepInterval]

### stepPixels: `number{:javascript}`[#stepPixels]
`default: 48`  

### steps: `number[]{:javascript}`[#steps]

### style: `SVGStyles | string{:javascript}`[#style]
스타일셋 혹은 class selector.  

### [tick](./linear/tick)
RealReport-Chart 라이브러리 클래스 모델의 최상위 base 클래스.

  

### [tickLabel](./linear/tickLabel)
RealReport-Chart 라이브러리 클래스 모델의 최상위 base 클래스.

  

### gap: `number{:javascript}`[#gap]
게이지 본체와의 간격.  
`default: 8` 픽셀. 

### visible: `boolean{:javascript}`[#visible]
표시 여부.\
모델에 따라 기본값이 다르다.
- [Legend Visible](https:/jsfiddle.net/gh/get/library/pure/realgrid/realchart-demo/tree/master/common/visible)  
