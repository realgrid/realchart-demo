## **gauge[type=clock]**
시계 게이지 모델.
## Properties

### active: `boolean{:js}`[#active]
시계 동작 여부.  
<DefaultValue value="true" contents=""></DefaultValue>
### bottom: `string | number{:js}`[#bottom]
plot 영역의 아래쪽 모서리와 widget 사이의 간격.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### centerX: `string | number{:js}`[#centerX]
게이지 중심 수평 위치.
픽셀 단위의 크기나, plot 영역 전체 너비에 대한 상대적 크기로 지정할 수 있다.  
<DefaultValue value="'50%'" contents=""></DefaultValue>
### centerY: `string | number{:js}`[#centerY]
게이지 중심 수직 위치.
픽셀 단위의 크기나, plot 영역 전체 높이에 대한 상대적 크기로 지정할 수 있다.  
<DefaultValue value="'50%'" contents=""></DefaultValue>
### duration: `number{:js}`[#duration]
Animation duration.  
<DefaultValue value="500" contents=""></DefaultValue>
### height: `string | number{:js}`[#height]
게이지 높이.
픽셀 단위의 고정 값이나, plot 영역에 대한 상태 크기롤 지정할 수 있다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### [hourHand](./clock/hourHand)
시침 설정 모델.  
<DefaultValue value="..." contents=""></DefaultValue>
### [label](./clock/label)
label  
<DefaultValue value="..." contents=""></DefaultValue>
### left: `string | number{:js}`[#left]
plot 영역의 왼쪽 모서리와 widget 사이의 간격.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### [minorTick](./clock/minorTick)
minor tick  
<DefaultValue value="..." contents=""></DefaultValue>
### [minuteHand](./clock/minuteHand)
분침 설정 모델.  
<DefaultValue value="..." contents=""></DefaultValue>
### name: `string{:js}`[#name]
게이지 이름.
동적으로 게이지를 다루기 위해서는 반드시 지정해야 한다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### [pin](./clock/pin)
pin  
<DefaultValue value="..." contents=""></DefaultValue>
### radius: `string | number{:js}`[#radius]
게이지 원호의 반지름.
픽셀 단위의 크기나, plot 영역 전체 크기(너비와 높이 중 작은 값)에 대한 상대적 크기로 지정할 수 있다.
<Content type="expression" value="'50%'"></Content>로 지정하면 plot 영역의 width나 height중 작은 크기와 동일한 반지름으로 표시된다.  
<DefaultValue value="'40%'" contents=""></DefaultValue>
### right: `string | number{:js}`[#right]
plot 영역의 오른쪽 모서리와 widget 사이의 간격.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### [rim](./clock/rim)
rim 설정 모델.  
<DefaultValue value="..." contents=""></DefaultValue>
### [secondHand](./clock/secondHand)
초침 설정 모델.  
<DefaultValue value="..." contents=""></DefaultValue>
### size: `string | number{:js}`[#size]
[width](#width) 와 [height](#height) 를 동시에 설정한다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### style: `SVGStyles | string{:js}`[#style]
스타일셋 혹은 class selector.  
- `SVGStyles{:js}` 

| Name | Type | Optional |  
| ----- | ----- | ----- |  
| fill | `string{:js}` | true |  
| fillOpacity | `string{:js}` | true |  
| fontFamily | `string{:js}` | true |  
| fontSize | `string{:js}` | true |  
| fontStyle | `string{:js}` | true |  
| fontWeight | `string{:js}` | true |  
| rx | `string{:js}` | true |  
| stroke | `string{:js}` | true |  
| strokeDasharray | `string{:js}` | true |  
| strokeWidth | `string{:js}` | true |  
| textAlign | `'center' \| 'left' \| 'right'{:js}` | true |<DefaultValue value="undefined" contents=""></DefaultValue>
### [tick](./clock/tick)
main tick.  
<DefaultValue value="..." contents=""></DefaultValue>
### [tickLabel](./clock/tickLabel)
tick label  
<DefaultValue value="..." contents=""></DefaultValue>
### time: `string | number | Date{:js}`[#time]
이 속성에 명시적으로 시간을 지정하면 현재 시간 대신 이 시각을 표시한다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### timezone: `number{:js}`[#timezone]
분단위로 시계에 표시할 지역의 timezone을 지정한다.
예) 뉴욕: -4 * 60, 파리: 2 * 60  
<DefaultValue value="undefined" contents=""></DefaultValue>
### top: `string | number{:js}`[#top]
plot 영역의 위쪽 모서리와 widget 사이의 간격.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### visible: `boolean{:js}`[#visible]
표시 여부.  
<DefaultValue value="true" contents=""></DefaultValue>
### width: `string | number{:js}`[#width]
게이지 너비.
픽셀 단위의 고정 값이나, plot 영역에 대한 상태 크기롤 지정할 수 있다.  
<DefaultValue value="undefined" contents=""></DefaultValue>