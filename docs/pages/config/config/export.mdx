## **export**
차트 내보내기 설정 모델
## Properties

### fileName: `string{:js}`[#fileName]
내보내기 버튼을 사용해 내보낸 차트에 사용할 확장자 없는 파일 이름.
export 함수를 호출할 때 fileName을 지정하지 않을 경우 이 속성이 적용된다.
fileName을 지정하지 않을 경우 realchart.type으로 다운로드 된다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### hideNavigator: `boolean{:js}`[#hideNavigator]
<Content type="keyword" value="true"></Content>로 지정하면 내보내기 결과에  가 포함되지 않는다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### hideScrollbar: `boolean{:js}`[#hideScrollbar]
<Content type="keyword" value="true"></Content>로 지정하면 내보내기 결과에 [AxisScrollBar](#AxisScrollBar) 가 포함되지 않는다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### hideZoomButton: `boolean{:js}`[#hideZoomButton]
<Content type="keyword" value="true"></Content>로 지정하면 내보내기 결과에 [ZoomButton](#ZoomButton) 가 포함되지 않는다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### menus: `ExportType[]{:js}`[#menus]
내보내기 메뉴에 포함할 export type  
<DefaultValue value="undefined" contents=""></DefaultValue>
### scale: `number{:js}`[#scale]
이미지의 scale  
<DefaultValue value="undefined" contents=""></DefaultValue>
### url: `string{:js}`[#url]
내보내기 도중 에러가 발생했을 때 처리할 서버 URL.
지정하지 않을경우 realchart 서버에서 처리합니다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### useLibrary: `boolean{:js}`[#useLibrary]
내보내기를 라이브러리를 사용하여 진행할지 여부 지정  
<DefaultValue value="undefined" contents=""></DefaultValue>
### visible: `boolean{:js}`[#visible]
내보내기 버튼 표시 여부 지정  
<DefaultValue value="undefined" contents=""></DefaultValue>
### width: `number{:js}`[#width]
너비, 지정한 너비에 맞춰 높이가 결정된다.  
<DefaultValue value="undefined" contents=""></DefaultValue>