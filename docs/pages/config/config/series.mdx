## [series[type=bar]](./series/bar)
Bar 시리즈.<br/>

수평 또는 수직 막대로 여러 값들을 **비교**하는 데 사용한다.
막대의 길이가 y값을 표시한다.<br/>
이 시리즈를 기준으로 생성되는 x축은 [category](/config/config/xAxis/category)이다.<br/>

* [data](#data) 는 아래 형식들로 전달할 수 있다.

###### 단일 값 및 값 배열
|형식|설명|
|---|---|
|y|단일 숫자면 y값. x 값은 순서에 따라 자동 결정.|
|[]|빈 배열이면 <Content type="keyword" value="null"></Content>. x 값은 순서에 따라 자동 결정.|
|[y]|값 하나인 배열이면 y값. x 값은 순서에 따라 자동 결정.|
|[x, y,]|두 값 이상이면 순서대로 x, y값.<br/> 또는 [xField](#xfield) 속성이 숫자이면 x값의 index. [yField](#yfield) 는 y값의 index.
 [colorField](#colorfield) 는 color값의 index.|

###### json 배열
|Series 속성|설명 |
|---|---|
| [xField](#xfield) |속성 값, 또는 <Content type="expression" value="'x', 'name', 'label'"></Content> 속성들 중 순서대로 값이 설정된 것이 x 값이 된다.|
| [yField](#yfield) |속성 값, 또는 <Content type="expression" value="'y', 'value'"></Content> 속성들 중 순서대로 값이 설정된 것이 y 값이 된다.|
| [colorField](#colorfield) |속성 값, 또는 <Content type="expression" value="'color'"></Content> 속성 값으로 데이터포인트의 개별 색상으로 지정된다.|
## [series[type=bargroup]](./series/bargroup)

## [series[type=bellcurve]](./series/bellcurve)

## [series[type=boxplot]](./series/boxplot)

## [series[type=bubble]](./series/bubble)
버블 시리즈.<br/>
x, y로 지정되는 위치와 z로 지정되는 크기 사이의 관계를 표시한다.
주로 원의 크기가 데이터포인트의 중요도를 나타낸다.<br/>
이 시리즈를 기준으로 생성되는 x축은 [linear](/config/config/xAxis/linear)이다.<br/>

* [data](#data) 는 아래 형식들로 전달할 수 있다.

###### 단일 값 및 값 배열
|형식|설명|
|---|---|
|y|단일 숫자면 y, z값. x 값은 순서에 따라 자동 결정.|
|[]|빈 배열이면 <Content type="keyword" value="null"></Content>. x 값은 순서에 따라 자동 결정.|
|[z]|단일 값 배열이면 y, z값. x 값은 순서에 따라 자동 결정.|
|[y, z]|두 값 배열이면 y값과 z값. x 값은 순서에 따라 자동 결정.|
|[x, y, z,]|세 값 이상이면 순서대로 x, y, z값.<br/> 또는 [xField](#xfield) 속성이 숫자이면 x값의 index. [yField](#yfield) 는 y값의 index. [zField](#zfield) 는 z값의 index.|

###### json 배열
|Series 속성|설명|
|---|---|
| [xField](#xfield) |속성 값, 또는 <Content type="expression" value="'x', 'name', 'label'"></Content> 속성들 중 순서대로 값이 설정된 것이 x 값이 된다.|
| [yField](#yfield) |속성 값, 또는 <Content type="expression" value="'y', 'value'"></Content> 속성들 중 순서대로 값이 설정된 것이 y 값이 된다.|
| [ㅋField](#ㅋField) |속성 값, 또는 <Content type="expression" value="'z'"></Content> 속성 값이 z 값이 된다.|
| [colorField](#colorfield) |속성 값, 또는 <Content type="expression" value="'color'"></Content> 속성 값으로 데이터포인트의 개별 색상으로 지정된다.|
## [series[type=bump]](./series/bump)
포함된 시리즈들의 y값들을 비교해서 순위로 시리즈를 표시한다.


포함된 시리즈들의 x값이 동일한 data point y값들을 비교해서 순위를 yValue로 재설정한다.
## [series[type=candlestick]](./series/candlestick)

## [series[type=circlebar]](./series/circlebar)

## [series[type=circlebargroup]](./series/circlebargroup)

## [series[type=dumbbell]](./series/dumbbell)
BarRange 시리즈 변종.
## [series[type=equalizer]](./series/equalizer)
Bar 시리즈 변종.
## [series[type=errorbar]](./series/errorbar)

## [series[type=funnel]](./series/funnel)

## [series[type=heatmap]](./series/heatmap)
heatmap 시리즈.

차트나 split pane에 하나의 heatmap만 존재할 수 있다.

[셀 색상]
1. color-axis가 연결되면 거기에서 색을 가져온다.
2. series의 minColor, maxColor 사이의 색으로 가져온다.
3. series의 기본 색상과 흰색 사이의 색으로 가져온다.
## [series[type=histogram]](./series/histogram)
각 bin은 하한값을 포함하고 상한값은 포함하지 않는다. 마지막 bin은 상한값을 포함한다.
X축이 <Content type="expression" value="'linear'"></Content>이어야 한다.
## [series[type=arearange]](./series/arearange)

## [series[type=area]](./series/area)
영역 시리즈.

대부분 [<Content type="expression" value="'line'"></Content>](/config/config/series/line) 시리즈와 동일하고 라인 아래 부분을 별도의 색상으로 채운다.
## [series[type=areagroup]](./series/areagroup)

## [series[type=line]](./series/line)
Line 시리즈.<br/>

주로 시간이나 다른 축 변수에 의한 데이터의 **변화** 또는 **경향**(pattern)을 보여주는 데 사용한다.
데이터포인트의 값에 해당하는 지점에 표시되는 데이터포인트 마커들을 연결한 선으로 표시되며,
기본 x축은 [linear](/config/config/xAxis/linear)이다.<br/>

* [data](#data) 는 아래 형식들로 전달할 수 있다.

###### 단일 값 및 값 배열
|형식|설명|
|---|---|
|y|단일 숫자면 y값.|
|[y]|값 하나인 배열이면 y값.|
|[x, y]|두 값 배열이면 순서대로 x, y값|
|[...]|시리즈 속성 값이 숫자인 경우 [xField](#xfield) 는 x값의 index. [yField](#yfield) 는 y값의 index.|

###### json 배열
 |Series 속성|설명|
|---|---|
| [xField](#xfield) |x 값. 기본값은 **<Content type="expression" value="'x'"></Content>**.|
| [yField](#yfield) |y 값. 기본값은 **<Content type="expression" value="'y'"></Content>**.|
## [series[type=linegroup]](./series/linegroup)

## [series[type=lollipop]](./series/lollipop)
Bar 시리즈 변종.
bar가 겹치더라도 데이터포인트들이 구분된다.
## [series[type=ohlc]](./series/ohlc)

## [series[type=pareto]](./series/pareto)
참조하는 원본 시리즈의 누적 비율을 표시한다.
## [series[type=pie]](./series/pie)

## [series[type=piegroup]](./series/piegroup)

## [series[type=scatter]](./series/scatter)

## [series[type=treemap]](./series/treemap)
treemap 시리즈.

차트나 split pane에 하나의 treemap만 존재할 수 있다.

1. 본래 하드 드라이브의 파일 분포 상태를 표시하기 위해 고안됨.
2. node & link 스타일의 전통적 표시 방식은 공간을 많이 필요로 한다.
3. 일정 표시 공간을 100% 사용한다.
4. 초기 공간을 재귀적으로 나누어 가면서 구성한다.
## [series[type=vector]](./series/vector)

## [series[type=waterfall]](./series/waterfall)

