## **xAxis[type=category]**
지정된 카테고리 개수로 축을 분할해서 각 카테고리에 연결된 데이터포인트들이 표시되게 한다.<br/>
카테고리 하나가 1의 너비를 갖는다.
주로 x축으로 사용되며, 선형(linear)축과 달리 축을 분할한 각 카테고리는 서로 격리되어 있으며, 
개별 축의 너비(간격)나 축들 사이의 순서가 의미가 없다.<br/>
또, 축 label에 카테고리를 대표하는 이름을 표시힐 필요한 경우 먼저 카테고리 축을 고려해야 한다.
 [startValue](#startValue) 부터 시작해서 [valueStep](#valueStep) 속성에 지정된 값씩 차례대로 증가한 값을 갖게된다. [categoryStep](#categoryStep) 설정값에 따라 기본적으로 동일하게 배분되고,  [categories](#categories) 속성으로 카테고리를 지정할 때, 상대적 크기를 width로 지정해서 각 카테고리의 값을 다르게 표시할 수 있다.
## Properties

### categories[#categories]
명시적으로 지정하는 카테고리 목록.<br/>
문자열로 카테고리 항목을 지정하거나,
object로 지정할 때에는 name(혹은 label) 속성에 카테고리 이름을 문자열로,
width 속성에 상대 너비(1이 기본 너비)를 숫자로 지정한다.
첫 번째 값이 [startValue](#startValue) 에 해당하고 [valueStep](#valueStep) 씩 증가한다.
각 카테고리의 상대적 너비를 지정할 수 있다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### categoryField: `string | number{:js}`[#categoryField]
카테고리로 사용되는 dataPoint 속성.<br/> [categories](#categories) 가 지정되면 이 속성은 무시된다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### categoryPadding: `number{:js}`[#categoryPadding]
각 카테고리의 양 끝에 추가되는 여백의 카테고리에 너비에 대한 상대적 크기.<br/>
0 ~ 0.5 사이의 값으로 지정한다.  
<DefaultValue value="0.1" contents=""></DefaultValue>
### categorySeries: `string{:js}`[#categorySeries]
Category 목록을 수집하는 시리즈.<br/>
지정하지 않으면 모든 시리즈에서 카테고리를 수집한다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### col: `number{:js}`[#col]
분할 모드일 때 축이 표시될 pane의 수평 위치.  
<DefaultValue value="0" contents=""></DefaultValue>
### [crosshair](./category/crosshair)
직선 또는 bar 형태로 축 위의 마우스 위치를 표시하는 구성 요소 모델.  

### [grid](./category/grid)
visible 기본값이 <Content type="keyword" value="undefined"></Content>이다.
visible이 <Content type="keyword" value="undefined"></Content>나 <Content type="keyword" value="null"></Content>로 지정되면, 축 위치에 따라 visible 여부가 결정된다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### [guides](./category/guides)
기본적으로 이 축에 연결된 모든 body에 모두 표시된다.
col, row를 지정해서 특정 body에만 표시되도록 할 수 있다.  

### [label](./category/label)
축 label 설정 모델.<br/>  

### [line](./category/line)
축 선(line) 설정 모델.<br/>  

### marginFar: `number{:js}`[#marginFar]
차트 경계나 뒤쪽 축 사이의 여백 크기.  
<DefaultValue value="0" contents=""></DefaultValue>
### marginNear: `number{:js}`[#marginNear]
plot 영역이나 먼저 표시되는 축 사이의 여백 크기.  
<DefaultValue value="0" contents=""></DefaultValue>
### maxPadding: `number{:js}`[#maxPadding]
축의 끝 카테고리 위치 이 후에 여백으로 추가되는 크기.<br/>
카테고리 기본 너비(1)에 대한 상대적 크기로 지정한다. [padding](#padding) 속성으로 양끝 padding을 한꺼번에 지정할 수 있다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### maxValue: `number{:js}`[#maxValue]
명시적으로 지정하는 최대값.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### minPadding: `number{:js}`[#minPadding]
축의 시작 카테고리 위치 이 전에 여백으로 추가되는 크기.<br/>
카테고리 기본 너비(1)에 대한 상대적 크기로 지정한다. [padding](#padding) 속성으로 양끝 padding을 한꺼번에 지정할 수 있다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### minValue: `number{:js}`[#minValue]
명시적으로 지정하는 최소값.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### name: `string{:js}`[#name]
<DefaultValue value="undefined" contents=""></DefaultValue>
### padding: `number{:js}`[#padding]
축의 양 끝 카테고리 위치 전후에 여백으로 추가되는 크기.<br/>
각각 시작/끝 카테고리에 대한 상대적 크기로 지정한다. [minPadding](#minpadding) , [maxPadding](#maxpadding) 으로 별도 지정할 수 있다.  
<DefaultValue value="0" contents=""></DefaultValue>
### position: `'base'|'normal'|'opposite'{:js}`[#position]
표시 위치.
기본적으로 상대 축의 원점 쪽에 표시된다.  
- `'base'` 상대 축의 baseValue 지점에 표시된다.      [주의]   1. 축에 연결된 시리즈들이 BarSeries 계열일 때만 가능하다.  2. 차트의 X축 하나에만 적용할 수 있다. 두번째로 지정된 축의 속성은 [NORMAL](#normal) 로 적용된다.  3. 상대 축이 **linear** 가 아니거나 [baseValue](/config/config/xAxis#basevalue) 가 min 보다 작거나 max보다 크면 이 값은 무시되고, [NORMAL](#normal) 로 적용된다.
- `'normal'` X축은 아래쪽에 수평으로, Y축은 왼쪽에 수직으로 표시된다.     [inverted](/config/config#inverted) 이면 Y축이 아래쪽에 수평으로, X축은 왼쪽에 수직으로 표시된다.
- `'opposite'` X축은 위쪽에 수평으로, Y축은 오른쪽에 수직으로 표시된다.     [inverted](/config/config#inverted) 이면 Y축이 위쪽에 수평으로, X축은 오른쪽에 수직으로 표시된다.

<DefaultValue value="'normal'" contents=""></DefaultValue>
### reversed: `boolean{:js}`[#reversed]
<Content type="keyword" value="true"></Content>면 반대 방향으로 point 위치들이 지정된다.  
<DefaultValue value="false" contents=""></DefaultValue>
### row: `number{:js}`[#row]
분할 모드일 때 축이 표시될 pane의 수직 위치.  
<DefaultValue value="0" contents=""></DefaultValue>
### [scrollBar](./category/scrollBar)
축 스크롤바 모델.<br/>
축 스크롤 상태를 표시하고, 사용자가 스크롤 범위나 위치를 변경할 수 있다.  

### [sectorLine](./category/sectorLine)
부채꼴 polar 좌표계의 X 축일 때 원호의 양 끝과 중심에 연결되는 선분들의 설정모델.<br/> [startAngle](/config/config/xAxis/category#startAngle) , [totalAngle](/config/config/xAxis/category#totalAngle) 을 참조한다.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### startAngle: `number{:js}`[#startAngle]
Polar 차트에서 사용될 때 시작 각도.  
<DefaultValue value="0" contents=""></DefaultValue>
### startOffset: `number{:js}`[#startOffset]
polar 축일 때 시작 위치 간격.<br/>
첫번째 카테고리 너비(각도)에 대한 상대값으로 0~1 사이의 값을 지정한다.
ex) 0.5로 지정하면 bar 시리즈의 첫 째 bar가 12시 위치에 표시된다.  
<DefaultValue value="0" contents=""></DefaultValue>
### style: `SVGStyles | string{:js}`[#style]
스타일셋 혹은 class selector.  
- `SVGStyles{:js}` 

| Name | Type | Optional |  
| ----- | ----- | ----- |  
| fill | `string{:js}` | true |  
| fillOpacity | `string{:js}` | true |  
| fontFamily | `string{:js}` | true |  
| fontSize | `string{:js}` | true |  
| fontStyle | `string{:js}` | true |  
| fontWeight | `string{:js}` | true |  
| rx | `string{:js}` | true |  
| stroke | `string{:js}` | true |  
| strokeDasharray | `string{:js}` | true |  
| strokeWidth | `string{:js}` | true |  
| textAlign | `'center' \| 'left' \| 'right'{:js}` | true |<DefaultValue value="undefined" contents=""></DefaultValue>
### [tick](./category/tick)
축에 표시되는 tick 위치와 표시 마크에 관한 설정 모델.  

### [title](./category/title)
축 타이틀 설정 모델.  

### tooltipFooter: `string{:js}`[#tooltipFooter]
축에 포함된 시리즈들 툴팁의 아래쪽에 표시되는 텍스트.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### tooltipHeader: `string{:js}`[#tooltipHeader]
축에 포함된 시리즈들 툴팁의 위쪽에 표시되는 텍스트.

tooltipHeader
tooltipRow,
tooltipRow,
...
tooltipFooter
형태로 툴팁이 표시된다.  
<DefaultValue value="'<b>${name}</b>'" contents=""></DefaultValue>
### tooltipRow: `string{:js}`[#tooltipRow]
축에 포함된 각 시리즈별 표시되는 포인트 툴팁 텍스트.  
<DefaultValue value="'${series}:<b>" contents="${yValue}</b>'"></DefaultValue>
### totalAngle: `number{:js}`[#totalAngle]
Polar 차트에서 사용될 때 원호 전체 각도.
0 ~ 360 사이의 값으로 지정해야 한다.
범위를 벗어난 값은 범위 안으로 조정된다.
지정하지 않거나 잘못된 값이면 360으로 계산된다.  
<DefaultValue value="360" contents=""></DefaultValue>
### unit: `string{:js}`[#unit]
label 등에 표시할 수 있는 단위 정보 문자열.  
<DefaultValue value="undefined" contents=""></DefaultValue>
### visible: `boolean{:js}`[#visible]
표시 여부.  
<DefaultValue value="true" contents=""></DefaultValue>