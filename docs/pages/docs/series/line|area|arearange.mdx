## series.line|area|arearange
undefined
## Properties

### firstVisible: MarerVisibility
첫번째 point의 marker 표시 여부.  
`default: MarerVisibility.DEFAULT`  

### lastVisible: MarerVisibility
첫번째 point의 marker 표시 여부.  
`default: MarerVisibility.DEFAULT`  

### maxVisible: MarerVisibility
최대값 point들의 marker 표시 여부.  
`default: MarerVisibility.DEFAULT`  

### minVisible: MarerVisibility
최소값 point들의 marker 표시 여부.  
`default: MarerVisibility.DEFAULT`  

### radius: number
`default: 4`  

### shape: Shape
명시적으로 지정하지 않으면 typeIndex에 따라 Shapes 중 하나로 돌아가면서 설정된다.  
