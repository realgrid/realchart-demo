import { RealChartReact } from "@/components/RealChart/RealChartReact";
import { config } from "@/templates/axis-break";

# Axis Breaks

<RealChartReact config={config} showEditor={true} autoUpdate={false} />
